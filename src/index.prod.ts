// ==========================================================
// STORESGO PRODUCTION ENTRY POINT
// Re-exports the main server.ts for production builds
// ==========================================================

// Import and run the full server from server.ts
// This ensures production uses the same code path as development
import "./server.js";
